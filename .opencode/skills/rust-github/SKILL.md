---
name: rust-github
description: Работа с GitHub в Rust проекте. Применять при пуше, создании релизов, работе с токеном и публикацией кириллицы.
---

# Rust — GitHub

- **Commit** — можно делать без явной команды «коммит» / «commit» / «сделай коммит», но коммит делать БЕЗ пуша!
- **Push** — только если дополнительно сказано «пуш» / «push» / «commit push». Без явной просьбы не делать никогда!
- «Сделай коммит» НЕ означает «сделай push». Никогда не делать пуш, если это явно не сказано именно про пуш.
- Если пользователь не сказал ни «коммит», ни «пуш» — то можно сделать коммит, но пуш — никогда!
- Напомнить о пуше после каждых 5 изменённых файлов. Если изменений меньше 5, но пользователь говорит «готово», «заканчиваем», «на сегодня хватит» — тоже спросить про пуш.
- Токен: `%USERPROFILE%\.github_token` (ограничен по времени).
- ✅ Для публикации релизов с кириллицей — `gh` CLI через `cmd.exe`, тело в файле UTF-8 без BOM, флаг `--notes-file`
- ❌ НЕ использовать PowerShell `Invoke-RestMethod` — он кодирует тело в Windows-1251, кириллица в release notes превращается в кракозябры
